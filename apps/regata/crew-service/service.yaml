apiVersion: v1
kind: Service
metadata:
  name: crew-service
  namespace: regatas
  labels:
    app: crew-service
spec:
  type: ClusterIP
  selector:
    app: crew-service
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
