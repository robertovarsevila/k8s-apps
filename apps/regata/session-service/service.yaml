apiVersion: v1
kind: Service
metadata:
  name: session-service
  namespace: regatas
  labels:
    app: session-service
spec:
  type: ClusterIP
  selector:
    app: session-service
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
