apiVersion: v1
kind: Service
metadata:
  name: analysis-service
  namespace: regatas
  labels:
    app: analysis-service
spec:
  type: ClusterIP
  selector:
    app: analysis-service
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
